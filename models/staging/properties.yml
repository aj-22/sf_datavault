version: 2

models:
  - name: staging__customer
    config:
      materialized: incremental
      unique_key: C_HASHKEY
  - name: staging__nation
    config:
      materialized: incremental
      unique_key: N_HASHKEY
  - name: staging__orders
    config:
      materialized: incremental
      unique_key: O_HASHKEY
  - name: staging__region
    config:
      materialized: incremental
      unique_key: R_HASHKEY