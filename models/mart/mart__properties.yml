version: 2

models:
  - name: mart__fact_orders
    config:
      materialized: table

  - name: mart__dim_customer
    config:
      materialized: table

  - name: mart__outrigger_geography
    config:
      materialized: table